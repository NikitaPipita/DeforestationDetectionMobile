<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deforestationdetectionmobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeforestationDetectionMobile">
        <activity android:name=".presentation.iot.ChangeIotState"></activity>
        <activity android:name=".presentation.iot.AllIotsList" />
        <activity android:name=".presentation.navigation.MainNavigationPage" />
        <activity android:name=".presentation.iot.AddIot" />
        <activity android:name=".presentation.iot.AddActivity" />
        <activity android:name=".presentation.iot.ActiveIotsList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>